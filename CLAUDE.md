@AGENTS.md

## Claude Code Notes

- Treat `AGENTS.md` as the shared entrypoint for all coding agents.
- Use `.claude/rules/*.md` as path-scoped reminders for Go code, tests,
  benchmarks, and documentation.
- Keep long-lived project rules in `.agents/project-standards.md`; keep
  `CLAUDE.md` short so session context stays focused.
